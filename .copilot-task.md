# Implement Event Master Data Management Feature

## Context and Current State

You are working on the **GTM Event Tracker** application - a centralized go-to-market event management system built on SAP Cloud Application Programming Model (CAP) with SAP Fiori Elements frontend, deployed on SAP BTP Cloud Foundry using SAP HANA database.

**CRITICAL: This is the FIRST business feature being implemented on a seed/template project.**

The project repository currently contains:
- A basic CAP project structure with `db/`, `srv/`, and `app/` folders
- Example/demo entities (likely Books, Authors, or similar seed data)
- A `package.json` with basic dependencies
- Basic configuration files

**YOU MUST:**
1. **First, examine all existing files** in the project to understand the current state
2. **REPLACE the seed/example entities** - do NOT create parallel structures
3. **Modify existing files** rather than creating new ones alongside examples
4. **Remove all demo/example data** and entities from the seed project
5. **Keep the project structure intact** - only change content, not structure

**What has been implemented:** Nothing yet - this is the first feature on a fresh seed project.

**What you will implement:** The foundation of the event management system, starting with the core Events entity and basic CRUD operations through a Fiori Elements UI.

## Feature Goal

Implement a complete event master data management capability that allows event planners to create, read, update, and delete events with essential attributes. This feature establishes the central Events entity as the foundation for the entire application.

## Your Task

Build a fully functional event management interface with backend data model, service layer, business logic, and Fiori Elements UI that enables users to manage go-to-market events through intuitive list and detail views.

## Implementation Instructions

### Step 1: Examine and Understand Current Codebase

**BEFORE making any changes:**
- Read and analyze all files in `db/` folder - identify what seed entities exist
- Read and analyze all files in `srv/` folder - identify what services are defined
- Read and analyze all files in `app/` folder - identify what UI apps exist
- Check `package.json` for current dependencies and scripts
- Understand the current project structure completely

**You MUST replace, not add to, the existing seed entities.**

### Step 2: Define the Events Entity Schema

**Modify** (do not create new) the file `db/schema.cds`:
- Remove all existing seed/example entities completely
- Define a new `Events` entity as the core business entity
- Include the following fields with proper CDS types:
  - `ID` as primary key using UUID type with automatic generation
  - `eventName` as String with maximum 200 characters, marked as mandatory
  - `eventDate` as Date type, marked as mandatory
  - `location` as String with maximum 200 characters
  - `status` as String with maximum 20 characters, with default value 'Planning'
  - `expectedGuests` as Integer type with default value 0
  - `description` as String with maximum 5000 characters for large text
  - Managed audit fields: `createdAt`, `createdBy`, `modifiedAt`, `modifiedBy` using CDS managed aspects

**Technical requirements:**
- Use proper CDS syntax with correct type annotations
- Apply the `@mandatory` annotation to required fields
- Use the `managed` aspect to automatically handle audit fields
- Ensure UUID generation is configured for the ID field
- Set appropriate default values where specified

**Consider querying the CAP/CDS MCP** if uncertain about:
- Proper syntax for managed aspects
- UUID generation patterns
- CDS type definitions and annotations

### Step 3: Replace Service Definition

**Modify** (do not create new) the service definition file in `srv/` folder (likely `*-service.cds`):
- Remove all existing seed service definitions completely
- Define a new service called `EventsService`
- Expose the `Events` entity from the database schema
- Enable draft functionality using the `@odata.draft.enabled` annotation
- Ensure full CRUD operations are enabled (set `@readonly: false` or ensure no restrictions)
- Define a custom type `StatusType` as an enumeration containing: Planning, Confirmed, Completed, Cancelled
- Apply this `StatusType` to the `Events.status` field

**Technical requirements:**
- Use proper CDS service syntax
- Correctly reference the database entity using namespace if needed
- Ensure the service is properly structured for OData exposure
- Make the status field use the enumerated type for validation

**Consider querying the CAP/CDS MCP** if uncertain about:
- Service definition patterns
- Draft enablement configuration
- Enum type definitions in CDS

### Step 4: Implement Service Business Logic

**Modify or create** the service implementation file in `srv/` folder (likely `*-service.js`):
- Remove any existing seed/example business logic
- Implement event handlers for data validation
- Register a BEFORE handler for CREATE and UPDATE operations on Events entity that:
  - Validates `eventName` is not empty or whitespace
  - Validates `eventDate` is not in the past for events with 'Planning' status
  - Validates `expectedGuests` is greater than or equal to zero
  - Uses `req.error()` to return meaningful error messages for validation failures
- Register a BEFORE handler for DELETE operations (can be simple or empty if no restrictions needed)

**Technical requirements:**
- Use proper CAP service handler registration syntax
- Access request data correctly from the `req` object
- Implement robust validation logic with clear error messages
- Handle edge cases (null values, undefined fields, etc.)
- Use asynchronous handler patterns if needed

**Consider querying the CAP/CDS MCP** if uncertain about:
- Handler registration patterns
- Request object structure
- Error handling best practices in CAP

### Step 5: Add UI Annotations for List Report

**In the service definition file** (`srv/*-service.cds`), add UI annotations to the Events entity:
- Define `@UI.SelectionFields` annotation to enable filtering by `status` and `eventDate`
- Define `@UI.LineItem` annotation to configure the list view columns with:
  - Event Name column displaying `eventName` field with label
  - Date column displaying `eventDate` field with label
  - Location column displaying `location` field with label
  - Status column displaying `status` field with label and optional criticality visualization
  - Expected Guests column displaying `expectedGuests` field with label
- Ensure column order matches the business priority
- Consider adding criticality calculation for status field (optional: create virtual element for color coding)

**Technical requirements:**
- Use proper Fiori Elements UI vocabulary annotations
- Structure LineItem as an array of DataField records
- Provide meaningful labels for end users
- Consider user experience in column ordering

**Consider querying the UI5 MCP or Fiori MCP** if uncertain about:
- UI vocabulary annotation syntax
- LineItem configuration options
- Criticality patterns

### Step 6: Add UI Annotations for Object Page

**In the service definition file** (`srv/*-service.cds`), add Object Page annotations to the Events entity:
- Define `@UI.HeaderInfo` annotation with:
  - TypeName as 'Event' and TypeNamePlural as 'Events'
  - Title referencing `eventName`
  - Description referencing `eventDate`
- Define `@UI.HeaderFacets` annotation for header area with:
  - A reference facet pointing to a FieldGroup for header key performance indicators
- Create a `@UI.FieldGroup#HeaderData` containing:
  - Status field
  - Expected Guests field
  - Location field
- Define `@UI.Facets` annotation for main content area with:
  - A reference facet labeled 'General Information' pointing to a FieldGroup
- Create a `@UI.FieldGroup#GeneralInfo` containing all event fields:
  - Event Name, Event Date, Location, Status, Expected Guests, Description

**Technical requirements:**
- Use proper Fiori Elements Object Page annotation patterns
- Structure facets and field groups correctly
- Ensure logical grouping of information
- Provide user-friendly labels

**Consider querying the Fiori MCP** if uncertain about:
- Object Page floorplan patterns
- HeaderFacets vs Facets usage
- FieldGroup definition syntax

### Step 7: Configure Value Help for Status Field

**In the service definition file** (`srv/*-service.cds`):
- Add a `@Common.ValueList` annotation to the `Events.status` field
- Configure the annotation to reference a collection path called 'StatusValues'
- Define parameters for InOut mapping between local `status` property and value list `code` property
- Create a virtual entity named `StatusValues` in the service with:
  - `code` field as String key
  - `name` field as String for display

**In the service implementation file** (`srv/*-service.js`):
- Implement a READ handler for the `StatusValues` entity
- Return a static array of status options:
  - Planning
  - Confirmed
  - Completed
  - Cancelled
- Each status should have both `code` and `name` properties

**Technical requirements:**
- Use proper Common vocabulary for ValueList annotation
- Ensure parameter mapping is correct for two-way binding
- Implement the value list handler to return data in expected format

**Consider querying the UI5 MCP or CAP/CDS MCP** if uncertain about:
- ValueList annotation structure
- Virtual entity patterns
- Value help implementation in CAP

### Step 8: Apply Field-Level UI Annotations

**In the service definition file** (`srv/*-service.cds`):
- Add `@title` annotations to all Events entity fields with user-friendly labels:
  - eventName: 'Event Name'
  - eventDate: 'Event Date'
  - location: 'Location'
  - status: 'Status'
  - expectedGuests: 'Expected Guests'
  - description: 'Description'
- Add `@UI.MultiLineText: true` annotation to the `description` field for proper text area rendering
- Ensure `@mandatory` annotations are present at service level for `eventName` and `eventDate`

**Technical requirements:**
- Apply annotations directly to field definitions or use annotate syntax
- Ensure consistency in labeling conventions
- Verify mandatory annotations are enforced

### Step 9: Generate Fiori Elements Application

**Use SAP Fiori CLI to generate the UI application:**
- Run the Fiori generator command (e.g., `fiori generate app` or equivalent)
- Select the 'List Report Object Page' floorplan template
- Bind the application to the `EventsService` service and `Events` entity
- Configure the application with:
  - Application title: 'Manage Events'
  - Appropriate namespace following project conventions
  - Routing configuration for navigation
- Review and adjust the generated `manifest.json` file if needed
- Ensure the application is correctly placed in the `app/` folder structure

**Technical requirements:**
- Use Fiori CLI rather than manual file creation
- Verify the generated application structure is correct
- Ensure proper service binding in manifest
- Check that UI annotations are being consumed correctly

**Consider querying the Fiori MCP** if uncertain about:
- Fiori CLI commands and options
- List Report Object Page configuration
- Manifest.json structure

### Step 10: Test the Application Locally

**Before deployment, verify the application works:**
- Use `cds watch` command to start the local CAP server with live reload
- Open the Fiori application in a browser
- Test all CRUD operations:
  - Create a new event with valid and invalid data (test validations)
  - View the list of events with filters
  - Open event details and verify all fields display correctly
  - Edit an event and save changes
  - Delete an event
  - Test status value help functionality
  - Verify mandatory field enforcement
- Check browser console for errors
- Verify OData service endpoints respond correctly

**Fix any issues before proceeding to deployment**

### Step 11: Prepare and Execute Deployment

**Configure deployment artifacts:**
- Ensure an `mta.yaml` file exists (create if needed) with modules for:
  - Application router (approuter) module
  - CAP service module (Node.js runtime)
  - Fiori application module (content deployed to HTML5 repo)
  - Database deployer module for HANA
- Configure service bindings for XSUAA, destination, HTML5 runtime, and HANA
- Verify build configuration in `package.json` includes necessary scripts

**Build and deploy:**
- Run `mbt build` to create the MTAR archive
- Run `cf deploy` to deploy to Cloud Foundry environment
- Monitor deployment progress and check for errors
- Verify all modules start successfully using `cf apps` and `cf services`

**Technical requirements:**
- Ensure proper module dependencies in mta.yaml
- Configure resource bindings correctly
- Verify deployment target (org and space) is correct

**If uncertain about MTA configuration**, examine existing mta.yaml structure or consult CAP deployment documentation

## Technical Guidance

**Architectural Decisions:**
- Use CDS managed aspects for audit fields rather than manual tracking
- Leverage draft functionality for better user experience during data entry
- Implement validation in the service layer, not the database layer
- Use enumerated types for status to ensure data consistency
- Apply UI annotations in service definition to keep UI logic centralized

**Best Practices:**
- Keep business logic in the service implementation layer
- Use proper error handling with meaningful messages
- Follow CAP and Fiori Elements naming conventions
- Ensure OData service is RESTful and follows standards
- Test locally before deploying to avoid cloud debugging

**Tool Usage:**
- Use `cds watch` for local development with auto-reload
- Use Fiori CLI (`fiori generate`, `fiori add`) for UI generation
- Use CDS CLI (`cds add`, `cds deploy`) for CAP operations
- Query MCPs when uncertain:
  - UI5 MCP for Fiori/UI5 API questions
  - CAP/CDS MCP for service and data model questions
  - Fiori MCP for Fiori Elements patterns and annotations

## Validation Checklist

Before considering this feature complete, verify:
- [ ] Events entity is defined in `db/schema.cds` with all required fields
- [ ] EventsService exposes Events with draft enabled in `srv/*-service.cds`
- [ ] Business logic validates eventName, eventDate, and expectedGuests
- [ ] Status field uses enumerated type with four defined values
- [ ] UI annotations enable list view with five columns
- [ ] UI annotations enable object page with header and general info sections
- [ ] Status field has working value help dropdown
- [ ] All fields have proper labels and mandatory annotations
- [ ] Fiori application is generated and bound to EventsService
- [ ] Application runs locally without errors using `cds watch`
- [ ] Users can create, read, update, and delete events successfully
- [ ] Validation errors display properly for invalid data
- [ ] Application deploys successfully to BTP Cloud Foundry
- [ ] Deployed application is accessible and functional

## Expected Outcome

Upon completion, the GTM Event Tracker application will have:
- A complete backend data model for Events with validation
- An OData service exposing full CRUD operations
- A Fiori Elements List Report showing all events with filtering
- A Fiori Elements Object Page showing detailed event information
- Working value help for status selection
- Full draft support for comfortable data entry
- Deployed and running on SAP BTP Cloud Foundry

Users will be able to manage go-to-market events from planning through completion with a professional, enterprise-grade user interface.

---

**Remember:** Always start by examining what currently exists in the codebase. Replace seed data, don't add to it. Use CLI tools and MCPs to accelerate development and ensure best practices. Test locally before deploying.